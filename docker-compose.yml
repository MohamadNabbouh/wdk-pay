services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: wdkpay
      POSTGRES_PASSWORD: wdkpay
      POSTGRES_DB: wdkpay
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
